# Agent guidance

Read [`.aicode/AGENTS.md`](.aicode/AGENTS.md) before working in this repository. It is the agent-maintained record of durable, repo-specific lessons and contains instructions for keeping itself current. A nested `AGENTS.md` is not discovered automatically when working from the repository root, so this pointer is required.

Current user instructions always take precedence.
